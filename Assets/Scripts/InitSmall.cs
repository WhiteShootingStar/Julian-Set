using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitSmall : MonoBehaviour
{
   public  Pixel[] pixels = new Pixel[400];
    public Pixel pixel;
    private float x;
    //private void Awake()
    //{
    //     x = transform.position.x;
    //    for (int i = 0; i < pixels.GetLength(0); i++)
    //    {
    //        var pix = Pixel.Instantiate(pixel, new Vector2(x, transform.position.y), transform.rotation);
    //        pixels[i] = pix;
    //        x += 0.1f;
    //    }
    //}
    //void Start()
    //{
        
       
    //}

    // Update is called once per frame
    void Update()
    {
        
    }
}
